// Package store provides the policy store registry.
// It defines the common interfaces and factory functions for
// policy store backends, allowing the system to switch between
// file, etcd, and MongoDB storage implementations at runtime.
package store
