// Package svcs provides shared service constants and utilities.
// It defines the REST API path prefixes, gRPC interceptors, and
// conversion helpers used by both the ADS and PMS service
// implementations across REST and gRPC transports.
package svcs
