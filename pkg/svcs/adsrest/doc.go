// Package adsrest implements the ADS REST API HTTP server.
// It exposes the authorization decision service over HTTP,
// handling is-allowed and discover-requests endpoints with
// configurable assertion and authorization logic.
package adsrest
