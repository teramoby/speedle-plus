// Package assertion implements token assertion and principal extraction.
// It supports multiple token types (JWT, OAuth2, custom) and provides
// pluggable assertion engines that validate tokens and extract
// principal information for use in authorization decisions.
package assertion
